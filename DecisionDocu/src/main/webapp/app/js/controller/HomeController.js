/**
 * Controller for the 'index.html' - no functionality (for tests - alert token)
 */
app.controller('HomeController', [
		'$scope',
		'$cookies',
		'$location',
		function($scope, $cookies, $location) {
		} ]);
